package com.cg.assignment;

public class Program3 {

	public static void main(String[] args) {
		 
				String string1="Java String pool refers to the collection of Strings which are stored in heap memory";
				
				//Lowercase
				String s2=string1.toLowerCase();
				System.out.println("Lowercase : "+s2);
				
				//Uppercase 
				String s3=s2.toUpperCase();
				System.out.println("Upper case : "+s3);
				
				


				//replace a with $
				String replaceString=string1.replace('a','$');
				System.out.println("Replaced string : "+replaceString);

				//contains
				if(string1.contains("collection"))
				{
					System.out.println( "found in string");
				}
				else{
					System.out.println("not found in string");
				}


				//matches
				System.out.print("Return Value :" );
				System.out.println(string1.matches("java string pool refers to the collection"
						+ " of strings which are stored in heap memory"));

	}
}
